# LiftLog Hard Copy Spike

This worktree is a short-lived LiftLog spike that keeps two preserved 3-variant hard-copy implementations behind one review shell.

Purpose: build a static web page that represents the latest LiftLog workout in a readable hard-copy format.

If the task is unclear, assume the goal is to surface the workout structure, last recorded values, and basic local-only editing for review.
